# Loaded automatically by `npm run build`. Safe to commit: this value is inlined
# into the client bundle at build time and is therefore public by definition.
VITE_API_URL=https://pikpilot.onrender.com
